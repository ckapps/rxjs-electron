/**
 * File containing mocks for electron modules
 */

// ==================================================
// Electron: app
// ==================================================
export const app = {
  whenReady: jest.fn(async () => {
    return void 0;
  }),
};
